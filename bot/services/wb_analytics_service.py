"""Бизнес-правила категорий из ТЗ (раздел 9): пороговые значения, используемые
при ручной оценке артикулов (create_action) и в будущих авто-подсказках.

Подключение WB API (реальные метрики CPL/маржи/остатков по артикулу) —
фаза 2. Пока это только именованные константы без логики: расчётов,
запросов к WB API или обращений к БД этот модуль не делает.
"""

# --- реклама (CPL, руб.) ---
TARGET_CPL = 20            # целевой CPL
CPL_RAISE_THRESHOLD = 15   # ниже этого порога CPL допустимо поднять ставку рекламы

# --- маржинальность ---
TARGET_CABINET_MARGIN = 0.40   # целевая маржа по кабинету в целом
TARGET_PRODUCT_MARGIN = 0.60   # целевая маржа по товару
LIQUIDATION_MARGIN = 0.30      # ниже этого порога — распродажа/ликвидация остатков

# --- сроки ---
STALE_DAYS = 5                 # артикул считается «зависшим» без продаж N дней
DECISION_COOLDOWN_DAYS = 3     # минимальный интервал между повторными решениями по артикулу
OVERSTOCK_DAYS = 120           # запас товара на складе (в днях оборота), после которого — затоваривание
